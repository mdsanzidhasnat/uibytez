import { Process } from "@/components/process";

export const metadata = {
  title: "Our Process — UIBytez",
  description:
    "How we turn ideas into successful digital products — from discovery to continuous growth.",
};

export default function ProcessPage() {
  return <Process />;
}
