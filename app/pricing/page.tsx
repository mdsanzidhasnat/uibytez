import { Pricing } from "@/components/pricing";

export const metadata = {
  title: "Pricing — UIBytez",
  description:
    "Transparent pricing packages for website design, business websites, e-commerce, and web apps. Get a free consultation for an accurate quote.",
};

export default function PricingPage() {
  return <Pricing />;
}
